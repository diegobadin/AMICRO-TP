{
  "name": "@unoarena/spectator",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Spectator View: a privacy-filtered Kafka projection and the SSE fan-out over it.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "ioredis": "^6.0.0",
    "kafkajs": "^2.2.4",
    "prom-client": "^15.1.3"
  }
}
