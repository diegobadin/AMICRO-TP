{
  "name": "@unoarena/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API gateway: the single entry point — JWT validation, routing, and the SSE tier.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "jose": "^6.2.8",
    "prom-client": "^15.1.3"
  }
}
