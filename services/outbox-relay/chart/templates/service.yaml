apiVersion: v1
kind: Service
metadata:
  name: {{ include "svc.name" . }}
  labels:
    app: {{ include "svc.name" . }}
spec:
  selector:
    app: {{ include "svc.name" . }}
  ports:
    - port: 80
      targetPort: {{ .Values.port }}
