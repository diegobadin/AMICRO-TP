{
  "name": "@unoarena/identity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Identity & Session placeholder with a real register/whoami slice (the fully-wired service).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/pg": "^8.21.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "ioredis": "^6.0.0",
    "jose": "^6.2.8",
    "kafkajs": "^2.2.4",
    "pg": "^8.22.0",
    "prom-client": "^15.1.3"
  }
}
