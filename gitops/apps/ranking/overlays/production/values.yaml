# Environment overlay for ranking (production). image.digest is pinned by the deploy-gitops CI job at
# deploy time — build once, promote the SAME digest from staging to production (no rebuild).
image:
  repository: registry.gitlab.com/REPLACE_GROUP/unoarena/ranking
  digest: "" # pinned by CI (deploy-gitops)
replicas: 3
logLevel: info
