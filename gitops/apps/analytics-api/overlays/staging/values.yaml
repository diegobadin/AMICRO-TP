# Environment overlay for analytics-api (staging). image.digest is pinned by the deploy-gitops CI job at
# deploy time — build once, promote the SAME digest from staging to production (no rebuild).
image:
  repository: registry.gitlab.com/REPLACE_GROUP/unoarena/analytics-api
  digest: "" # pinned by CI (deploy-gitops)
replicas: 1
logLevel: debug
