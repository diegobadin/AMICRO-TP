apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tournament-staging
  namespace: argocd
spec:
  project: unoarena
  destination:
    server: https://kubernetes.default.svc
    namespace: unoarena-staging
  sources:
    - repoURL: REPLACE_REPO_URL
      targetRevision: main
      path: services/tournament/chart
      helm:
        valueFiles:
          - $values/gitops/apps/tournament/overlays/staging/values.yaml
    - repoURL: REPLACE_REPO_URL
      targetRevision: main
      ref: values
  syncPolicy:
  automated:
    prune: true
    selfHeal: true
    syncOptions:
      - CreateNamespace=true
